.worktrees/
.plans/
.claude/worktrees/
.claude/plans/
.claude/settings.local.json
.env
.env.*

# Generated by flow/skills/maintain/scripts/check.sh — check.sh writes to
# "$(pwd -P)/.cenci/maintain-report.json", which can be any project subdir
# (e.g. watch/.cenci/), so this must match at any depth, not just repo root.
**/.cenci/maintain-report.json

# Transient per-run pipeline state (ticket #558/#559)
.cenci/pipeline/

# Go
watch/cenci
watch/dist/

# Node
node_modules/

# IDE
.idea/
.vscode/
