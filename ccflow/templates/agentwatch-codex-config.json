{
  "agents": {
    "codex": {
      "command": "codex",
      "model": "gpt-5-codex",
      "workflows": {
        "implement": {
          "args": [
            "exec",
            "Implement GitHub ticket #{ticket} end-to-end. Follow the ccflow workflow documented in AGENTS.md: work in a git worktree, write failing tests first, implement the simplest passing code, refactor, self-review (security / code quality / silent failures), then open a PR with gh. 1 ticket = 1 PR; never commit to main."
          ]
        }
      }
    }
  }
}
