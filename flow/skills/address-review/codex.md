# Codex address-review procedure

Read `project-core`, `codex-runtime`, and `pr-comment-filter`. Fetch unresolved threads,
evaluate actionability, and checkpoint the proposed fixes. Obtain approval in `/plan`; in a
second normal-mode invocation apply approved fixes, test, commit, push without force, reply,
and re-request review. Never resolve disputed feedback silently.
