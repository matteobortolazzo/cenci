# Codex garden procedure

Read `project-core` and `codex-runtime`. Audit AGENTS.md critical rules and topic docs with
read-heavy workers. Present the curation plan in `/plan`; after approval apply it in an
isolated worktree, test documentation contracts, and open one reviewed PR.
